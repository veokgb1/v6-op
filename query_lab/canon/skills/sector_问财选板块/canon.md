# 问财选板块 法典

**skill_key**: sector  
**skill_name_zh**: 问财选板块  
**版本**: v1（初始）  
**状态**: 空法典，等待实测后通过 `canon promote` 写入

## 说明

本法典记录同花顺问财中用于选板块的中文问法。

**注意**：板块查询可能需要 `sector` 或 `zhishu` fallback，
`actual_query_backend` 字段会记录实际调用的问财类型。

## 危险词（禁止使用）

以下词语模糊、主观，禁止自动生成：

- 热门板块
- 强势板块
- 主线板块
- 龙头板块
- 可能爆发的板块

## 当前条目

*法典为空。请运行实测后执行 `canon propose` → `canon lint` → `canon promote`。*

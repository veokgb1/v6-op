# V6OP 第二阶段升级大纲

> 文件性质：第二阶段正式纲要  
> 接续文件：`F:\v.6\v6-op\v6-op_full_rescue_charter.md`  
> 讨论依据：`F:\v.6\v6-op\docs\architecture_discussion_tmp\phase1_cleanup_phase2_method\rounds\022_claude_analyst_response_phase2_final_artifacts.md`  
> 日期：2026-05-06

---

## 一、第二阶段总宗旨

**让用户在操盘时，能知道自己在相信什么。**

更具体地说：

- 当系统说“K线已够用”，K线必须真的是按用户设置的天数准备好的。
- 当用户改了参数，结果必须确实基于新参数计算。
- 当问财返回 45 只，报告必须说清楚为什么是 45。
- 当最终命中为 0，系统必须告诉用户是哪一层把结果变成了 0，以及下一步应该调什么。
- 当页面展示诊断和数据来源时，必须服务于当前操盘动线，不能把单页操盘台做成资料博物馆。

第二阶段不是新增功能堆叠阶段，也不是零散补 bug 阶段。它的目标是让第一阶段已经建立的运行骨架变得可解释、可追踪、可相信。

---

## 二、第二阶段为什么存在

第一阶段的结论是：

**可运行骨架 + 关键链路待稳定化。**

第一阶段已经真实建立了这些能力：

- 单页操盘台。
- 股票来源、技能、路径、结果报告的基本链路。
- 问财、全A、手动等来源入口。
- 缠论、K线、SMC、波浪、排雷等 live 能力入口。
- sequential / parallel_and / simple_hybrid 三条路径。
- 两阶段数据隔离、K线预取、技能结果缓存、运行报告。

但真实使用暴露出八类问题：

1. 用户设置 `days=730`，系统内部仍可能按默认 365 天计算。
2. 问财返回 45 只时，用户不知道这是正常结果、接口限制、授权问题，还是查询过严。
3. 命中 0 时，用户不知道是来源为空、K线不足、技能过严、路径交集为空，还是报告解释不足。
4. “K线已够用”的判断可能基于固定 `365d.pkl`，不是用户实际设置的天数。
5. 问财授权机制不透明，`api_key` 是否真正生效需要代码级验证。
6. 页面承载内容过重，操盘内容、管理内容、帮助内容、历史内容混在一起。
7. abort / 清缓存 / 续跑没有明确后端语义。
8. 三条执行路径的真实代码逻辑与报告表达式不完全在同一路上。

第二阶段存在的理由，就是把这些问题收束成一套稳定机制，而不是继续哪里痛补哪里。

---

## 三、第二阶段不是什么

第二阶段不是新增打分制、抗压模型、新灰色技能接入或复杂策略市场。

第二阶段不是单点 bug 修复清单。它必须建立三套基础设施：

- 透明度机制：解释为什么这样。
- 分类机制：主操盘台和管理中心分流。
- Bridge 能力：问财既能做来源，也能对已有股票池做外部验证/标签。

第二阶段也不是把单页操盘台做成博物馆。Data Provenance、Bridge Capability、分层解释器都必须在当前运行结果内折叠展示，不能为每个概念另开独立浏览页。

---

## 四、第二阶段 8 个大目标

### G1 参数-数据-缓存全链路一致化

用户设置的 `days` 必须从参数、K线取数、分析计算、缓存指纹一路传到底。K线“够用”的判断必须基于用户实际设置的天数，不得固定依赖 `365d.pkl`。

### G2 全局解释层与分层归零诊断

建立五层解释契约：来源层、数据层、技能层、路径层、报告层。每层必须输出输入数量、产出数量、失败原因、建议操作。命中 0 时，系统必须说明是哪一层导致清零。

### G3 来源层语义清晰化与诊断字段保留

PRO 5000 在问财模式和全A模式下必须显示不同语义。问财 0、少量返回、接近上限三种场景必须有不同解释。`query_text`、`actual_count`、`api_called`、`elapsed_s` 等字段不得在 `source_resolver` 丢弃。

### G4 问财授权验证与 Bridge 双角色定义

必须验证问财授权在清洁环境下是否真实生效。问财必须被定义为 Bridge Capability：开头可以生成股票池，中后段可以对已有股票池做外部验证或标签。

Bridge 至少包含两种模式：

- Constrained mode：`result = wencai_query ∩ existing_pool`。
- Second-pass mode：`final_list = skill_filtered -> wencai_annotate(hit_list)`，只附加标签，不改变命中列表。

### G5 数据血缘层定义与取数透明化

每次取数必须记录 provider、是否新拉、是否降级、最新日期、失败数量、旧数据使用情况。报告和页面必须展示本轮数据来源摘要，不能让用户从日志里猜。

### G6 主操盘台与管理中心分流

主操盘台只保留操盘必需内容，管理功能进入管理中心。管理中心不能拥有启动分析能力。

主操盘台严格只保留 7 项：

1. 股票来源选择，含 PRO 5000 语义提示、Bridge 模式选项。
2. 技能开关与参数调节。
3. 执行路径选择。
4. 启动按钮和实时进度状态区。
5. 命中结果列表。
6. 本轮全局解释，五层诊断，折叠展示。
7. 本轮数据血缘摘要，折叠展示。

管理中心承接：

- 历史运行记录。
- 分层缓存清理。
- 系统设置。
- 帮助文档。
- 问财授权状态检查。

### G7 运行管理语义正式化

abort 是步骤边界停止，不是强制中断。清缓存分为三层：来源快照、K线数据库、技能结果库。历史运行记录可以恢复参数到主操盘台，但不能自动启动运行。

### G8 执行路径统一

`sequential` 和 `simple_hybrid` 必须逐步改为走 `expression_runner`，与 `parallel_and` 对齐。报告中的 `expression_spec` 应从描述性字符串升级为真正驱动执行的结构化规格。G8 必须最后做，出现严重回归时可以回退 G8，但不能永久移出第二阶段。

---

## 五、Pass 关系

第二阶段允许按 Pass 组织，但 Pass 不是缩范围，也不是三个新阶段。它们共同组成一个完整第二阶段。

### Pass 1：信任基础主链

覆盖：G1、G3、G4-auth、G2、G5。

顺序：G1 -> G3 -> G4-auth -> G2 -> G5。

完成后用户应能看到：

- 改 `days` 有实际效果。
- 问财返回 0、45、接近上限都有解释。
- 命中 0 有五层定位。
- 报告有数据血缘摘要。

### Pass 2：结构与架构

覆盖：G6、G7、G4-Bridge、G8。

顺序：G6 -> G7 -> G4-Bridge -> G8。

完成后用户应能看到：

- 主操盘台轻量清晰。
- 管理中心独立。
- Bridge 两种模式可用。
- 三条执行路径表达式统一。

### Pass 3：验收补漏

Pass 3 不新增目标，只处理：

- 文案修正。
- 边界字段补齐。
- 漏测场景补测。
- G8 的轻微回归修复。
- 如 G8 在 Pass 2 已被回退，则 Pass 3 单独处理 G8 并重新验收。

---

## 六、Data Provenance 契约

Data Provenance 是本轮运行的数据来源说明，不是历史数据浏览库。

每只股票至少记录：

```text
source
is_new_fetch
is_degraded
latest_date
trust_level
```

本轮汇总至少记录：

```text
fetch_mode
total
fetched_new
from_cache
failed
degraded
oldest_data_date
```

展示位置：

- `run_report.json` 或等效报告对象中的 `data_provenance`。
- 主操盘台结果区内的折叠摘要。
- 日志中保留关键 provider 与失败信息。

---

## 七、分层问题定位解释器契约

命中为 0 或结果极少时，必须按五层解释：

1. 来源层：股票池从哪里来，实际返回多少，是否调用外部接口。
2. 数据层：K线是否检查，本地多少，新拉多少，失败多少，最新日期是什么。
3. 技能层：每个技能输入多少，命中多少，失败多少，哪个技能最先筛空。
4. 路径层：顺序漏斗哪一步清零，并行交集为什么为空，排雷剔除了多少。
5. 报告层：最终解释是否完整，下一步建议是什么。

用户最终看到的不是“命中 0”四个字，而是“为什么命中 0”。

---

## 八、第二阶段验收原则

第二阶段完成必须通过这些场景：

- 问财返回 0：来源层解释清楚，`why_zero` 指向来源层。
- 问财返回少量，如 45：显示为正常返回，不误报失败。
- 问财接近上限：提示可能触发截断。
- 全A 5000：文案明确是本地名单截取，不与问财混淆。
- `days=365` 与 `days=730`：报告记录实际使用天数，缓存指纹不互相命中。
- K线本地已有、新拉、失败、旧数据：Data Provenance 都有记录。
- 技能筛空：解释器能指出具体技能。
- 顺序路径筛空：解释器能指出哪一步清零。
- 并行交集为空：解释器能指出交集为空。
- Bridge constrained：结果是已有池的子集。
- Bridge second-pass：命中列表不变，只附加标签。
- abort：在步骤边界停止。
- 三层缓存清理：三层互不误伤。
- 管理中心没有启动运行入口。
- 主操盘台只保留 7 项操盘内容。
- G8 完成后，三条路径走 `expression_runner` 且无回归。

---

## 九、第二阶段关闭标准

第二阶段关闭时，必须有一份第二阶段过程报告，至少包含：

1. G1-G8 完成状态。
2. Pass 1 / Pass 2 / Pass 3 的实际执行情况。
3. 修改文件清单。
4. 关键数据结构字段清单。
5. 页面分流结果。
6. Bridge 模式实现状态。
7. Data Provenance 实现状态。
8. 五层解释器实现状态。
9. 验收命令和结果。
10. 如果仍有未完成项，必须说明是实现阻塞还是验收失败，不得写成已完成。


# v6-op 全量救场总章程

---

## 第一部分：章程正文

---

### 一、为什么要做 v6-op

**V6 不是失败的项目，而是一个走偏了方向的项目。**

V6 已经建立了 V5 完全没有的能力：技能卡体系、蒙版模型、蒙版表达式、追踪账本、命中解释、报告组。这些东西在概念上是真实的进步。V5 只能做线性漏斗，V6 的蒙版并线和自由组合是 V5 做不到的。

但 V6 在建立这些能力的过程中，走进了一个陷阱：**每增加一个新概念，就新建一个页面；每新建一个页面，就新增一套账本。** 结果是：用户打开 V6，看到的不是一个可以操盘的工具，而是一座展示系统架构的博物馆。8 个 workbench 页面、11 个账本目录、75 份架构文档、20 份 schema，每一个都有它的道理，但用户找不到一条从"打开页面"到"得到今天命中股票"的清晰动线。

与此同时，V6 的核心数据层从未真正建立。所有的蒙版运算、命中列表、技能执行，在第一阶段都依赖 fixture 文件里的固定代码，没有接通真实市场数据。用户看到的命中结果，是样本数据的排列，不是今天真实的选股结论。

V5 虽然旧，但它解决了这个问题。它有单页操盘入口，用户进来就知道从哪里开始。它有批量网络拉取、本地缓存、本地分析，能在 10 分钟内处理 5000 只股票。它的参数可以直接调，调完能跑，跑完能看。V5 的漏斗虽然是旧结构，但漏斗至少是真的在跑，结果是真实的。

**v6-op 的使命是：让 V6 的蒙版能力和 V5 的可用能力在同一个系统里同时存在。**

它不是推翻 V6。V6 的后端模型是对的，继续有效。它不是复刻 V5。V5 的固定漏斗不是目标，只是参考。它是：**在 V6 的蒙版引擎上，重建一个以"每天能用"为第一优先的操盘驾驶舱。**

现有 V6 不能继续直接改，原因不是代码不好，而是它的页面哲学已经固化成"每个概念对应一个页面"。在这个框架里继续改，引力始终把每个新功能拉向"新建一个概念页面"。必须换一个容器，重新以"操盘动线"为组织原则。

---

### 二、v6-op 的定位

**v6-op 是 V6 的可用主线，是操盘驾驶舱。**

关系定义：
- **现有 V6** = 引擎房 + 后台能力库。它的后端模型（蒙版、表达式、账本）继续运转，不推翻，不废弃。
- **v6-op** = 驾驶舱。用户每天进来的地方。一个页面，一条动线，从选股票来源到看命中结果。

v6-op 对用户隐藏所有内部概念。用户不需要知道什么是 Mask，不需要知道什么是 ScopeRef，不需要知道什么是 ExecutionDraft，不需要理解账本目录结构。用户只需要知道：选来源、选技能、调参数、启动、看结果。

v6-op 不是 V7。它没有新的架构主张，不引入新框架，不创造新概念。它借用 V6 已经建立的后端能力，借用 V5 已经验证的执行机制，把它们重新组织成一个用户能每天使用的操盘工具。

---

### 三、现有 V6 如何处理

**分三类，界限清晰，不混。**

**冻结：**
- 现有 8 个 workbench 页面（skills / compose / execute / explain / report-group / closure / test-lanes / debug）的扩张立刻停止
- 不再新建概念页面
- 不再新增账本目录（11 个已有账本保持现状，不扩张到第 12 个）
- 不再新增 schema 文件
- 不再新增架构文档（75 份已够）
- 现有 V6 workbench 继续运行，作为调试和后台参考，不再作为用户主入口

**保留为后台能力，由 v6-op 直接引用：**
- `contracts.py`：Universe、Mask、MaskExpression 领域模型，设计稳定，直接引用
- `expression_runner.py`：12 种蒙版操作符，经测试可用，直接引用
- `mask_store.py`：蒙版持久化，直接引用
- `selection_condition.py`：条件执行框架，接口合理，引用并适配
- `mask_expression_executor.py`：表达式执行器，引用
- 技能卡资产（metadata、命中方向、参数定义）：作为技能工具箱的数据来源
- 蒙版模型、表达式组合能力：v6-op 所有路径的后台表达基础

**第一阶段暂不依赖的展示逻辑：**
- 现有 workbench 多页面的 UI 代码
- closure 闭环清单
- status_signoff 打标系统
- 独立的 explain 页面
- 独立的 report-group 页面
- 历史账本的展示界面
- 75 份文档的内容（作为参考，不作为依赖）

---

### 四、V5 应该继承什么

继承 V5 的**可用能力**，不继承 V5 的漏斗池结构。固定 6 槽漏斗不是目标，是被替代的对象。以下四层能力必须进入 v6-op：

**1. 单页操盘 UX 哲学**

V5 的 index.html 是单页 React SPA，进来就能用。v6-op 必须有同样的感觉：
- 左侧：股票来源选择 + 技能选择 + 参数调节区
- 中间：运行进度日志（实时滚动）
- 右侧：命中结果 + 解释 + 失败摘要

用户进来，五分钟内能启动一次完整运行，看到真实结果。不需要阅读文档，不需要理解系统架构。

**2. 网络拉取能力**

V5 的两阶段隔离必须进入 v6-op：
- 阶段一（网络）：问财取名单 → 批量预热 K 线缓存 → 写入本地 pickle
- 阶段二（本地）：所有分析技能切换为 READ_CACHE_ONLY，不碰网络

预热机制：8-worker 协调者模式。协调者把股票列表 round-robin 分成 8 份，启动 8 个子进程，每个子进程独立开 baostock 会话处理自己那份，写入 pickle，上报完成数量。

baostock 连接复用：每个子进程启动时全局 login() 一次，之后对 bs.login / bs.logout 应用猴子补丁（替换为空函数），所有股票复用这一个连接，结束时恢复并 logout()。这将 N×2 次 TCP 操作压缩为每子进程 2 次，防止 Windows TIME_WAIT 端口耗尽。

三源降级：baostock → akshare → yfinance → 旧缓存（带警告）。失败信息逐股记录，汇总进预热报告。

**3. 本地分析能力**

V5 的五个本地分析模块，逻辑移植为 V6 producer 接口：
- 缠论（CZSC）：buy point 检测，信号在最近 N 根 K 线内才保留
- SMC 聪明钱：BOS / ChoCH / FVG 信号，支持三种模式（严格过滤 / 软过滤标记 / 旁路透传）
- K 线形态：实体比、影线比、形态匹配，近 N 根有形态才保留
- 波浪分析：Elliott Wave 拟合，摆动窗口 + Fibonacci 容差参数
- 排雷：ST / 退市 / 次新股（上市 < 60 天）/ K 线不足（< 60 根），命中方向为负向（命中 = 剔除）

每个 producer 接口统一：输入 stock_codes + 本地缓存路径 → 输出 Mask（hit_codes、miss_codes、evidence、hit_semantics）。

**4. 报告输出能力**

V5 的最终报告格式是目标参考：中文可读、逐股条目、参数记录、证据附列、数据时效标注。第一阶段可以简化，但不能缺失。每次运行必须留下一份完整的运行记录，包含：本次参数、命中列表、证据摘要、失败情况、数据覆盖情况。

---

### 五、第一阶段必须交付什么

**必须交付清单：**

页面：
- [ ] 单页操盘台（v6-op 主页），用户全流程在此页完成，无需跳转

股票来源（三选一）：
- [ ] 全 A 股（加载 ashare_codes.txt）
- [ ] 问财自然语言查询（输入策略语句，调用问财 API，结果作为全集）
- [ ] 手动输入代码（粘贴或上传文件）

核心技能（6 个，真实执行，不用 fixture）：
- [ ] 问财（自然语言选股，轻量条件）
- [ ] 缠论（本地 K 线分析，CZSC 买点）
- [ ] SMC 聪明钱（本地 K 线分析，BOS/ChoCH/FVG）
- [ ] K 线形态（本地 K 线分析，形态识别）
- [ ] 波浪分析（本地 K 线分析，Elliott Wave）
- [ ] 排雷（本地元数据过滤，负向技能）

参数调节（第一阶段验收标准，不是后续美化）：
- [ ] 每个技能展开后有参数输入区
- [ ] 所有参数有默认值并可以修改
- [ ] 参数调整后重新运行，结果发生变化
- [ ] 参数值在结果报告里记录

执行路径（三种）：
- [ ] 顺序漏斗：技能依次过滤，上游输出是下游输入
- [ ] 并线取交集：多个技能对同一全集运行，结果取 AND
- [ ] 简单混合：前 N 个技能并线取交集，交集结果再顺序过滤

数据层：
- [ ] 真实 K 线预热（8-worker，非 fixture）
- [ ] 预热进度条实时显示
- [ ] 预热报告（总数 / 缓存命中 / 新拉 / 失败 / 耗时）

执行层：
- [ ] SSE 或等价实时进度日志（运行中实时滚动）
- [ ] 本地分析阶段只读缓存，不碰网络

结果层：
- [ ] 命中股票列表（真实股票，真实数据）
- [ ] 每只股票命中哪些技能，命中原因（中文）
- [ ] 点开单只股票展开完整证据
- [ ] 失败 / 未分析股票单独列出（不混入命中列表）
- [ ] 旧缓存使用情况显示（哪些股票用了过期数据）
- [ ] 本次路径类型和参数记录在报告里

---

### 六、第一阶段明确不做什么

以下内容明确排除在第一阶段之外，防止再次膨胀：

- 不接通全部 27 张技能（其余 21 张灰色显示或保留后台，标"暂未接通"）
- 不做任意 DAG 编辑器（不提供用户拖线连接的 UI）
- 不开放全部 12 种蒙版表达式操作符（第一阶段只自动生成表达式，不让用户手写）
- 不做完整报告组体系（报告简化版，不做分章节报告组）
- 不做独立 explain 页面（证据折叠在命中列表里，不跳新页）
- 不做独立 closure 页面
- 不做 status_signoff 打标系统
- 不接财务数据、公告数据、新闻数据、事件数据、指数环境数据
- 不做完整技能生命周期管理页面（raw / debug / prod / live 四段）
- 不继续扩张现有 V6 的 workbench 页面
- 不重新设计 V6 的后端架构
- 不引入 Airflow、Dagster、Prefect、Bazel、Feast 等外部框架

---

### 七、技能卡和技能管理怎么保留

27 张技能卡是 V6 已经建立的真实资产，不废弃，但它们的呈现方式必须改变。

**从"独立页面"变成"主页内嵌工具箱"：**
- 技能工具箱作为左侧面板的一个折叠区域存在，不是独立页面
- 用户组合策略时，从工具箱里勾选或拖入技能
- 工具箱展开后显示全部 27 张技能的列表

**每张技能卡在工具箱里至少显示：**
- 名称（中文）
- 是否真实接通（绿色 = 可用真实数据 / 灰色 = 暂未接通）
- 命中方向（+ 正向保留 / − 负向排除 / ○ 中性标注）
- 可调参数名称（折叠，点开显示输入框）
- 需要的数据类型（K 线日线 / 无需缓存 / 其他）
- 输出类型（股票代码列表 / 板块列表 / 其他）

**第一阶段的接通状态：**
- 绿色可用：问财、缠论、SMC、K 线形态、波浪、排雷（6 个）
- 灰色保留：其余 21 张技能，metadata 完整保留，真实执行能力待后续阶段接通

**系统责任：**
用户每天操盘时不需要进技能卡管理页面，但系统必须持续维护技能资产：接通状态、参数定义、命中方向。这些数据存在 v6-op 的技能注册表里，不是每次运行重新定义。

---

### 八、路径执行层怎么定

**第一阶段固定支持三种路径，三种路径共用一套执行引擎。**

三种路径的结构定义：

**顺序漏斗（线性链）：**
全集 → 技能 A → 输出蒙版 A → 技能 B（以蒙版 A 的 hit_codes 为输入）→ 输出蒙版 B → 技能 C（以蒙版 B 为输入）→ 最终命中

**并线取交集（分叉后合并）：**
全集 → 技能 A（全集）+ 技能 B（全集）+ 技能 C（全集）→ AND 合并 → 最终命中

**简单混合（先并线再顺序）：**
全集 → 技能 A（全集）+ 技能 B（全集）→ AND 取交集 → 中间结果 → 技能 C（中间结果）→ 技能 D（技能 C 输出）→ 最终命中

**执行层共同原则：**
- 三种路径都转换为后台的小型策略依赖图（节点 = 技能运行或合并操作，边 = 依赖关系）
- 系统对依赖图做拓扑排序，决定执行顺序；同层无依赖节点可并行
- 不为三种路径写三套执行代码；路径选择只影响图的形状，不影响执行引擎
- 蒙版表达式由系统根据路径选择和技能命中方向**自动生成**，用户不手写表达式
- 排雷等负向技能，系统自动将其接入 EXCLUDE 操作符，而不是 AND

**第一阶段不做：**
- 用户自定义 DAG 编辑器
- 超出简单混合的复杂图形（如多层并线、条件分支、循环）

---

### 九、数据拉取层怎么定

**核心原则：两阶段隔离，统一预热，技能只声明需求不自己拉。**

**固定流程：**

```
① 用户提交策略
② Fetch Planner 读取策略
③ 运行轻量条件（问财 / 手动输入）→ 确定 scope_codes
④ 汇总重量技能的数据需求声明 → 合并去重
⑤ 如 scope_codes 超出安全阈值（如 500 只），提示用户
⑥ 调用 data_prefetch（8-worker 协调者）→ 写入 pickle 缓存
⑦ 输出预热报告（总数 / 命中缓存 / 新拉 / 旧缓存 / 失败 / 耗时）
⑧ 所有重量技能切换为 READ_CACHE_ONLY
⑨ 按策略图拓扑顺序执行重量技能
```

**技能数据需求声明（最小字段）：**
- `data_type`：`"kline_daily"` 或 `"none"`（第一阶段只有这两种）
- `lookback_days`：需要多少个交易日历史（如 90）
- `hit_semantics`：`"positive"` / `"negative"` / `"neutral"`
- `is_lightweight`：`True` = 可在预热前运行（问财 / 手动属于此类）

**Fetch Planner 输出：**
- `scope_codes`：最终待分析的股票代码列表
- `prefetch_report`：总数 / cache_hit / fetched_ok / stale_used / failed / failed_codes / stale_codes / duration
- `readiness`：`"ready"` / `"partial"` / `"aborted"`
- 失败率超过 20% 时，readiness = aborted，整批终止

**失败如何显示给用户（命中列表同屏展示）：**
```
本次分析覆盖：265 只（完整数据）
使用旧缓存（昨日数据）：12 只  ▶ 展开
未能分析（数据源失败）：10 只  ▶ 展开
─────────────────────────
最终命中：23 只
```

展开后显示具体股票代码和失败原因，默认折叠不干扰主结果。

**第一阶段不支持的数据类型：**
财务数据、事件数据、公告数据、新闻数据、实时盘中行情、板块成分动态、指数环境数据。对应技能灰色显示。

---

### 十、实现原则：内容寻址缓存 + 拓扑排序执行

这两条是**实现原则**，不是新的架构层，不需要新建专门模块。

**内容寻址缓存（Mask 复用判断）：**

每次技能运行前，计算本次运行的稳定指纹：

| 字段 | 说明 |
|---|---|
| `skill_id` | 技能唯一标识 |
| `scope_codes` | 输入股票代码列表（排序后元组，保证顺序一致） |
| `params` | 技能参数（序列化后按 key 排序） |
| `data_date` | K 线缓存最新数据的日期（不是运行日期） |
| `algo_version` | 技能算法版本（技能文件里的常量字符串，算法改了手动递增） |

指纹 = SHA256(以上字段拼接)。指纹命中且未过期则直接复用 Mask，否则重算并写入缓存。

不用运行时间戳。时间戳会导致每次运行都是 cache miss。

**拓扑排序执行：**

策略图的节点和边在运行前一次性构建，然后做拓扑排序得到执行序列，同层节点可并行调度。漏斗、并线、混合三种路径都是这个图的不同形状，不需要三套执行代码。第一阶段不提供用户手绘 DAG 的 UI，图由系统根据路径选择自动生成。

**一句话总结：**
内容寻址缓存回答"这个结果还能不能用"，拓扑排序回答"这些技能该按什么顺序跑"——两个问题都不再需要为每种路径写一套专门的判断代码。

---

### 十一、参数面板必须写进验收标准

**参数调节是第一阶段必须交付的核心能力，不是后续美化项。**

v6-op 第一阶段的用户体验目标，明确参照 V5 左侧参数区：每个技能展开后有清晰的参数输入区，用户可以看到当前值、修改它、看到它影响结果。

**第一阶段必须包含的参数（对应 V5 已有参数）：**

| 技能 | 参数 | 说明 |
|---|---|---|
| 缠论 | signal_bars | 近 N 根 K 线内有买点才保留（默认 5） |
| 缠论 | buy_type | 一买 / 二买 / 三买 / 全部 |
| SMC | signal_bars | 近 N 根有信号才保留（默认 15） |
| SMC | swing_length | Swing 窗口大小（默认 10） |
| SMC | mode | 严格过滤 / 软过滤标记 / 旁路透传 |
| K 线形态 | signal_bars | 近 N 根有形态才保留（默认 5） |
| K 线形态 | body_pct | 实体比例（默认 0.1） |
| K 线形态 | shadow_ratio | 影线比（默认 2.0） |
| 波浪 | signal_bars | 近 N 根有信号才保留（默认 20） |
| 波浪 | swing_window | 摆动窗口（默认 10） |
| 波浪 | fib_tolerance | Fibonacci 容差（默认 0.15） |
| 排雷 | （无可调参数，规则固定） | ST / 退市 / 新股 / K 线不足 |
| 股票来源 | 问财策略 | 自然语言策略输入框 |
| 股票来源 | 扫描上限 | 问财返回最多 N 只（默认 300） |

**要求：**
- 所有参数有默认值，页面加载即显示
- 参数修改后，重新运行，结果可以发生变化（这是验收标准）
- 本次运行使用的参数值，完整记录在结果报告里
- 参数区不需要用户每次手动重填（上次使用的值默认回显）

---

### 十二、报告输出目标

**V5 的报告格式是最终目标参考，第一阶段可以简化，但不能缺席。**

**第一阶段最低输出要求（每次运行产出）：**

- 最终命中股票（代码 + 名称 + 综合命中情况）
- 每只股票命中了哪些技能（列表，带简短信号描述）
- 本次使用的参数（完整记录，可以对比不同次运行）
- 每只股票的命中证据（可展开，信号描述）
- 每只股票被哪个技能排除（如被排雷剔除，原因是什么）
- 数据覆盖情况（共分析多少只，失败多少只，旧缓存多少只）
- 本次选择的路径类型（漏斗 / 并线 / 混合）
- 未能分析的股票列表和原因

**后续阶段目标（不在第一阶段里，但方向明确）：**
- 逐步接近 V5 的整齐中文报告格式（Markdown 可读，可直接复制使用）
- 综合评分（缠论 + SMC + K 线 + 波浪 + 风险加权）
- 按评分排序的候选名单
- 历史报告对比

---

### 十三、第一阶段验收标准

以下清单全部通过，第一阶段方算完成：

1. 打开 v6-op 单页，无需跳转其他 V6 页面即可完成全部操作
2. 可以输入问财策略语句，系统调用真实问财 API，返回股票代码列表
3. 可以手动粘贴股票代码，系统直接使用该列表作为全集
4. 在界面上选择至少两个技能（如缠论 + SMC + 排雷）
5. 调整至少一个参数（如 SMC 的 signal_bars 从 15 改为 10）
6. 启动运行后，系统先显示"准备数据"进度（真实预热 K 线缓存，非 fixture）
7. 缓存就绪后系统自动开始分析，页面显示实时进度日志
8. 分析完成后显示真实命中股票列表（不是 fixture 数据）
9. 每只命中股票有中文命中解释（命中了哪些技能，发现了什么信号）
10. 失败 / 未分析的股票单独列出，不混入命中列表
11. 将 SMC signal_bars 改回 15 并重新运行，命中列表与第一次可以不同（证明参数真实有效）
12. 本次使用的参数值出现在结果报告里
13. 全流程不读取、不显示、不泄露任何 API 密钥
14. V5.10 目录下的文件未被修改（只读参考）
15. 用户在整个操作过程中，不需要理解 Mask、ScopeRef、ExecutionDraft 等 V6 内部术语

---

---

## 第二部分：复用导向开发计划

---

### 第一张表：V6 资产继承表

| 模块 / 资产 | 原位置 | 当前作用 | v6-op 使用方式 | 原因 |
|---|---|---|---|---|
| `contracts.py` | `scripts/v6/` | Universe、Mask、MaskExpression 领域模型 | **直接引用** | 设计稳定，是 v6-op 所有蒙版操作的基础对象 |
| `expression_runner.py` | `scripts/v6/` | 12 种蒙版操作符的内存计算 | **直接引用** | 测试通过，表达式评估完整可用 |
| `mask_store.py` | `scripts/v6/` | 将 Mask / Universe / Expression 写入文件系统 | **直接引用** | 持久化逻辑独立，不需要改动 |
| `selection_condition.py` | `scripts/v6/` | 单个条件执行框架（scope + method + output） | **适配包装** | 接口设计合理，但 stub producer 要替换为真实实现 |
| `selection_group.py` | `scripts/v6/` | 并行执行 1-4 个条件 | **适配包装** | 并行逻辑可复用，调度细节需适配新执行引擎 |
| `mask_expression_executor.py` | `scripts/v6/` | 读取 masks + draft，执行表达式 | **适配引用** | 表达式执行逻辑可用，数据输入来源改为真实 Mask |
| `mask_producers.py` | `scripts/v6/` | 技能目录 + stub/live producer 注册 | **只读参考（目录部分）** | 技能 metadata 和目录结构有价值；stub producer 实现不用，替换为真实分析模块 |
| `io_utils.py` | `scripts/v6/` | JSON / codes 文件读写工具 | **直接引用** | 文件 I/O 工具函数，通用可用 |
| `universe_provider.py` | `scripts/v6/` | 从代码文件构建 Universe 对象 | **直接引用** | 逻辑简单，直接可用 |
| `secret_masking.py` | `scripts/v6/` | 输出中脱敏 API Key | **直接引用** | 安全要求，必须保留 |
| `live_recapture.py` | `scripts/v6/` | 真实 API 请求构建 + 响应分类 | **适配引用** | 问财等真实接口的调用逻辑在这里，需要适配到 v6-op 的 Fetch Planner |
| 技能卡 metadata | `data/raw_skill_sample_cards.json` 等 | 27 张技能卡定义 | **直接引用** | 技能名称、参数定义、命中方向等 metadata 是技能工具箱的数据来源 |
| 现有 workbench 8 页面 | `web/v6-workbench-v2/*.html` | 现有 V6 操盘台 UI | **冻结，不依赖** | 页面哲学（概念导向）与 v6-op（操作导向）不兼容，不复用 |
| `workbench_server.py` | `scripts/v6/` | HTTP 服务器 + 本地账本刷新 | **只读参考** | 服务器架构参考，v6-op 重写服务器，但参考其 API 设计模式 |
| closure / status_signoff | `scripts/v6/` | 阶段闭环清单、状态打标 | **暂不依赖** | 第一阶段不进入主动线，后续阶段按需接入 |
| `v6_main_chain.py` | `scripts/v6/` | 端到端证明账本 | **暂不依赖** | 第一阶段不需要形式化证明，后续阶段考虑 |
| `report_group_summary.py` | `scripts/v6/` | 中文报告章节生成 | **只读参考** | 中文报告格式是参考目标，第一阶段简化版参考其输出格式 |
| JSON schema 定义 | `data/schemas/` | 20+ 数据结构 schema | **只读参考** | schema 设计作为数据结构参考，不作为 v6-op 的运行时依赖 |
| 测试 fixture | `tests/fixtures/` | 固定代码列表 + 样本数据 | **暂不依赖（单元测试可用）** | 第一阶段验收必须用真实数据，fixture 只在单元测试里使用 |

---

### 第二张表：V5 精华继承表

| 资产 | 原位置（V5.10a） | 当前作用 | v6-op 继承方式 | 禁止复制的内容 |
|---|---|---|---|---|
| `shared_data.py` | `scripts/` | 三源降级 OHLCV 拉取 + baostock 单进程连接 + pickle 缓存 | **提取核心逻辑，重写为 `ohlcv_provider.py`** | 不复制 .env，不复制 .venv，不复制 baostock API key 引用方式 |
| `prefetch_kline_cache.py` | `scripts/` | 8-worker 协调者 + 并行 K 线预热 + 报告汇总 | **提取调度逻辑，重写为 `data_prefetch.py`** | 不复制 .env 中的路径配置，路径在 v6-op 里重新定义 |
| 猴子补丁思想 | `step_smc_batch.py` | baostock login/logout 替换为 noop，复用单 TCP 连接 | **借鉴模式，在 `data_prefetch.py` 里重新实现** | 只借鉴思路，不复制整个 batch 文件（其中含策略执行逻辑） |
| Pickle 缓存结构 | `var/cache/kline_daily/` | 按股票代码存储日线数据，TTL 24 小时 | **沿用同样目录约定和文件格式** | 不复制 V5 的 var 目录本身（包含历史缓存和路径配置） |
| 三源降级逻辑 | `shared_data.py` | baostock → akshare → yfinance → stale cache | **提取降级链逻辑，集成进 `ohlcv_provider.py`** | 不复制 V5 的降级参数（如具体 timeout 值）照搬，重新校验 |
| CZSC 买点检测逻辑 | `step2_czsc_analysis.py` | 对每只股票跑 CZSC 买点，过滤近 N 根无信号的 | **提取核心分析逻辑，包装为 `CZSCProducer`** | 不复制 V5 的 slot 输入输出接口（替换为 V6 Mask 接口） |
| SMC 信号检测逻辑 | `step5_smc_analysis.py` | BOS/ChoCH/FVG 检测，三种过滤模式 | **提取核心分析逻辑，包装为 `SMCProducer`** | 同上 |
| K 线形态识别逻辑 | `step7_kline_pattern.py` | 形态匹配 + 实体/影线比过滤 | **提取核心识别逻辑，包装为 `KlineProducer`** | 同上 |
| 波浪分析逻辑 | `step8_wave_analysis.py` | Elliott Wave 拟合 + 摆动窗口 + Fibonacci 容差 | **提取核心分析逻辑，包装为 `WaveProducer`** | 同上 |
| 排雷过滤逻辑 | `step4_landmine_filter.py` | ST / 退市 / 次新股 / K 线不足过滤 | **提取过滤规则，包装为 `LandmineProducer`（负向）** | 同上 |
| SSE 日志流 | `web_server.py` | 前端实时接收后端进度日志 | **借鉴 SSE 实现模式，在 v6-op server 里重新实现** | 不复制 V5 完整的 web_server（其中含 V5 专用路由和配置） |
| 左侧参数面板 UX | `web/index.html` | 每个 slot 展开参数，调完即跑 | **借鉴布局理念，v6-op 重新设计 UI** | 不复制 HTML 代码（V5 前端代码与 V6 技能体系不兼容） |
| V5 报告输出格式 | `step6_final_report.py` + output | 中文整齐报告，含评分、参数、证据 | **作为格式目标参考，第一阶段简化实现** | 不复制 V5 的报告生成逻辑（与 V5 的 slot 结构强耦合） |
| `.env` / `.venv` | 根目录 | API 密钥 + Python 环境 | **绝对禁止复制** | 含真实 API 密钥，安全红线 |
| V5 漏斗池结构 | `pipeline_orchestrator.py` | 6-slot 固定顺序执行 | **不继承** | v6-op 底层用依赖图 + 拓扑排序，漏斗只是一种图形，不是架构 |
| `ashare_codes.txt` | `data/` | 全 A 股代码列表 | **直接复制文件（纯数据，无密钥）** | — |
| `skill_registry.py` | `scripts/` | 22 个技能的注册表（V5 视角） | **只读参考** | V5 的技能注册格式和 V6 不同，不直接复用，参考 metadata 结构 |

---

### 第三张表：对象接力开发表

每一步写清楚：输入是什么，输出是什么，复用哪里，需要新写什么，验收标准是什么。

---

**步骤 1：股票来源 → scope_codes**

| 项目 | 内容 |
|---|---|
| 输入 | 用户在页面上的选择：问财策略语句 / 手动代码列表 / 全 A 股标志 |
| 输出 | `scope_codes: list[str]`，如 `["600519", "000858", "300750"]` |
| 复用 V5 | `step1_wencai_merged.py` 的问财 API 调用逻辑 → 提取为 `wencai_source.py` |
| 复用 V6 | `universe_provider.py`（从代码列表构建 Universe 对象）|
| 需新写 | v6-op 的 `source_resolver.py`：接收用户选择，调用对应来源，输出统一的 `scope_codes` |
| 验收标准 | 输入问财策略，能返回真实股票代码列表；输入手动代码，能正常解析；全 A 股能加载 `ashare_codes.txt` |

---

**步骤 2：技能声明 → 数据需求清单**

| 项目 | 内容 |
|---|---|
| 输入 | 用户选择的技能列表 + 每个技能的参数值 |
| 输出 | `DataRequirement[]`：每个技能声明的 `{data_type, lookback_days, hit_semantics, is_lightweight}` |
| 复用 V6 | 技能卡 metadata（命中方向、参数定义）|
| 需新写 | 每个 producer 类的 `data_requirements` 属性声明；`SkillRegistry` 在 v6-op 里的注册逻辑 |
| 验收标准 | 选缠论 + SMC + 排雷，能正确输出三个技能各自的数据需求声明，且排雷标注为 `is_lightweight=False`，`hit_semantics="negative"` |

---

**步骤 3：scope_codes + 数据需求 → Fetch Planner → 预热清单**

| 项目 | 内容 |
|---|---|
| 输入 | `scope_codes`（轻量条件输出）+ `DataRequirement[]` |
| 输出 | `prefetch_list: {codes: list[str], data_type: str, lookback_days: int}[]`，合并去重后的预热任务 |
| 复用 V5 | `prefetch_kline_cache.py` 的任务分配逻辑（round-robin 分片）|
| 需新写 | `fetch_planner.py`：读入需求，过滤轻量条件，合并重量技能的 K 线需求，输出预热清单 |
| 验收标准 | 三个技能（缠论 + SMC + 排雷）对同一 scope，只生成一份合并后的 K 线预热清单，不重复 |

---

**步骤 4：预热清单 → data_prefetch → pickle 缓存**

| 项目 | 内容 |
|---|---|
| 输入 | `prefetch_list`（股票代码 + 数据类型 + 历史天数）|
| 输出 | 本地 pickle 缓存（`var/cache/kline_daily/{code}.pkl`）+ `PrefetchReport` |
| 复用 V5 | `prefetch_kline_cache.py` 的 8-worker 协调者模式 + 猴子补丁思想 + 三源降级 |
| 需新写 | `data_prefetch.py`：在 v6-op 里重新实现协调者-工作者，路径配置适配 v6-op 目录 |
| 验收标准 | 300 只股票能在 60 秒内完成预热（含缓存命中），输出报告包含 cache_hit / fetched_ok / failed 三个数字，失败股票有代码记录 |

---

**步骤 5：scope_codes + pickle 缓存 → SkillProducer → Mask**

| 项目 | 内容 |
|---|---|
| 输入 | `codes: list[str]`（当前 scope）+ 本地缓存路径 + 技能参数 |
| 输出 | `Mask {hit_codes, miss_codes, evidence, hit_semantics}` |
| 复用 V5 | 缠论：`step2_czsc_analysis.py` 核心逻辑；SMC：`step5_smc_analysis.py`；K线：`step7_kline_pattern.py`；波浪：`step8_wave_analysis.py`；排雷：`step4_landmine_filter.py` |
| 复用 V6 | `contracts.py` 的 Mask 对象；`selection_condition.py` 的条件执行框架（适配包装）|
| 需新写 | 每个 Producer 类（`CZSCProducer`、`SMCProducer`、`KlineProducer`、`WaveProducer`、`LandmineProducer`）：把 V5 分析逻辑包装进统一的 V6 Mask 接口 |
| 验收标准 | 给 CZSCProducer 传入 50 只股票代码和本地缓存路径，能返回有真实 `hit_codes` 和 `evidence` 的 Mask 对象，不报错，不依赖网络 |

---

**步骤 6：多个 Mask + 路径选择 → 策略图 → MaskExpression（自动生成）**

| 项目 | 内容 |
|---|---|
| 输入 | 技能输出的多个 Mask + 用户选择的路径类型（漏斗 / 并线 / 简单混合）|
| 输出 | `MaskExpression`（自动生成，用户不手写）|
| 复用 V6 | `contracts.py` 的 MaskExpression 对象和操作符定义 |
| 需新写 | `strategy_graph_builder.py`：根据路径类型生成策略依赖图；`expression_auto_generator.py`：根据图 + hit_semantics 自动组合操作符（正向用 AND，负向用 EXCLUDE） |
| 验收标准 | 选"并线 + 排雷"，系统自动生成 `EXCLUDE(AND(缠论蒙版, SMC蒙版), 排雷蒙版)`，用户不需要手动选操作符 |

---

**步骤 7：MaskExpression → ExpressionRunner → final hit_codes**

| 项目 | 内容 |
|---|---|
| 输入 | `MaskExpression` + 所有 Mask 的 `mask_registry`（mask_id → hit_codes 映射）|
| 输出 | `final_hit_codes: list[str]` |
| 复用 V6 | `expression_runner.py`（直接引用，无需改动）|
| 需新写 | 无。ExpressionRunner 已经可用，直接调用 |
| 验收标准 | 给 ExpressionRunner 传入 `AND(maskA, maskB)` 和两个真实 Mask，能返回正确的交集代码列表 |

---

**步骤 8：final_hit_codes + 各 Mask evidence → 中文命中解释**

| 项目 | 内容 |
|---|---|
| 输入 | `final_hit_codes` + 每个 Producer 的 `evidence`（信号描述字典）|
| 输出 | 每只股票的中文命中解释：`{code: "600519", name: "贵州茅台", hits: ["缠论: 近3根K线有二买信号", "SMC: 发现 BOS 向上突破"], excluded_by: []}` |
| 复用 V5 | `step6_final_report.py` 的中文描述生成逻辑（作为格式参考）|
| 复用 V6 | `report_group_summary.py` 的中文报告格式（作为参考）|
| 需新写 | `explanation_builder.py`：遍历 final_hit_codes，从各 Mask 的 evidence 里提取相关证据，生成中文解释条目 |
| 验收标准 | 命中列表里每只股票有至少一条中文解释，说明是哪个技能发现了什么信号 |

---

**步骤 9：命中解释 + PrefetchReport → 报告输出**

| 项目 | 内容 |
|---|---|
| 输入 | 命中列表（含中文解释）+ 预热报告 + 本次参数记录 + 路径类型 |
| 输出 | 运行结果对象（前端展示）+ 本次运行 JSON 记录（持久化）|
| 复用 V5 | V5 报告格式（作为目标参考，第一阶段简化版）|
| 复用 V6 | `mask_store.py`（保存最终蒙版）；`io_utils.py`（写 JSON）|
| 需新写 | `run_report.py`：组装运行结果，输出前端可渲染的结构 + 写入本次运行记录文件 |
| 验收标准 | 运行结束后能生成一份包含"命中列表 / 参数记录 / 数据覆盖 / 失败情况"的完整 JSON 运行记录，不依赖 fixture |

---

**步骤 10：失败信息 → 结果可信度摘要**

| 项目 | 内容 |
|---|---|
| 输入 | `PrefetchReport`（失败代码列表、旧缓存代码列表）|
| 输出 | 结果页面的可信度摘要区域（四行数字 + 可展开详情）|
| 复用 V5 | V5 的失败统计输出格式（参考）|
| 需新写 | 前端展示逻辑（将失败分类显示为"完整分析 / 旧缓存 / 未分析"三类）|
| 验收标准 | 即使有 10 只股票数据失败，这 10 只股票在结果页面里有明确标注，不混入命中列表，也不静默消失 |

---

**步骤 11：状态记录（暂简化）**

| 项目 | 内容 |
|---|---|
| 输入 | 本次运行结果 |
| 输出 | 运行记录 JSON 文件（存档，供后续对比）|
| 复用 V6 | `mask_store.py` 的文件写入逻辑 |
| 暂不做 | 完整 V6 账本体系 / status_signoff / 报告组分章 / closure 闭环 |
| 验收标准 | 每次运行后，`output/runs/` 目录里有对应 JSON 文件记录本次运行的参数和结果 |

---

### 阶段开发计划

**核心原则：每一阶段先复用，再新增；每一阶段有明确验收；不允许从空白开始。**

---

#### 阶段 0：数据引擎移植（先跑通数据，再碰 UI）

**目标：** 让 v6-op 能用真实数据预热 K 线缓存，并让一个 Producer 用缓存数据跑出真实 Mask。

**要做：**
- 新建 `v6-op/` 文件夹，基础目录结构（不照抄 V6 的 11 个账本目录）
- 移植 V5 `shared_data.py` 核心 → `v6-op/scripts/ohlcv_provider.py`（三源降级 + baostock 单连接）
- 移植 V5 `prefetch_kline_cache.py` 核心 → `v6-op/scripts/data_prefetch.py`（8-worker 协调者 + 猴子补丁）
- 移植 V5 `step2_czsc_analysis.py` 核心分析逻辑 → `v6-op/scripts/producers/czsc_producer.py`（包装为 V6 Mask 接口）
- 直接复制 V5 `data/ashare_codes.txt`（无密钥，纯数据）

**不能做：**
- 不碰任何 UI
- 不新建账本目录
- 不复制 `.env` / `.venv`
- 不修改 V5.10 任何文件

**验收：**
- 命令行跑 `data_prefetch.py`，传入 50 只股票代码，能在 30 秒内完成预热，输出 `prefetch_report.json`
- 命令行跑 `czsc_producer.py`，传入预热后的 50 只代码，能输出包含真实 `hit_codes` 和 `evidence` 的 Mask JSON

**风险点：** V5 的 `shared_data.py` 可能依赖 `.env` 里的路径变量，移植时需要把路径变量改为 v6-op 自己的目录约定

---

#### 阶段 1：6 个 Producer 全部接通

**目标：** 全部 6 个核心技能在真实 K 线数据上能产出真实 Mask。

**要做：**
- 移植并包装 SMC → `smcproducer.py`
- 移植并包装 K 线形态 → `kline_producer.py`
- 移植并包装波浪 → `wave_producer.py`
- 移植并包装排雷 → `landmine_producer.py`（`hit_semantics = "negative"`）
- 实现 `wencai_source.py`（调用真实问财 API，输出 `scope_codes`）
- 实现 `source_resolver.py`（统一处理三种股票来源）

**不能做：**
- 不做任何页面
- 不做 Fetch Planner
- 不做 UI

**验收：**
- 每个 Producer 单独接收 50 只真实股票代码，能跑出有内容的 Mask，不崩溃，不依赖网络（在已预热缓存上）
- 排雷 Producer 的 `hit_semantics` 为 `"negative"`，其命中的股票是"有风险应剔除"的

**风险点：** SMC 和波浪依赖第三方库（`smartmoneyconcepts`、自定义波浪库），需要先确认这些库在 v6-op 的 Python 环境里可以安装

---

#### 阶段 2：Fetch Planner + 执行引擎 + 后端 API

**目标：** 后端能接收策略配置，自动完成预热 + 执行 + 返回命中结果，无 UI 但有 API。

**要做：**
- `fetch_planner.py`：读取技能列表 + scope，输出预热清单
- `strategy_graph_builder.py`：根据路径类型生成策略依赖图
- `expression_auto_generator.py`：根据图 + hit_semantics 自动生成 MaskExpression
- 直接引用 V6 `expression_runner.py`（不修改）
- `explanation_builder.py`：从 evidence 生成中文解释
- `run_report.py`：组装运行结果 JSON
- `v6op_server.py`：Flask / SimpleHTTP 服务器，提供 `POST /api/run`（接收策略）+ `GET /api/stream`（SSE 进度）+ `GET /api/result`（返回结果）

**不能做：**
- 不做前端页面
- 不做任意 DAG 编辑器
- 不开放 12 种操作符给用户

**验收：**
- 用 curl 或 Postman 发送 `POST /api/run`，传入"问财 + 缠论 + SMC + 排雷，顺序漏斗"配置
- 能在 3 分钟内返回包含真实命中股票、中文解释、失败摘要的 JSON 结果
- SSE 流在运行中实时输出进度

**风险点：** 问财 API 的 Key 管理方式需要沿用 V6 的"Key 只在内存不落盘"原则，不能硬编码进代码

---

#### 阶段 3：单页操盘台前端

**目标：** 用户打开浏览器，能在一个页面完成完整选股操盘，所有阶段 2 的能力通过 UI 可用。

**要做：**
- `v6op/web/index.html`：单页操盘台
  - 左侧：股票来源选择 + 技能工具箱（6 个核心技能，勾选 + 展开参数）+ 路径选择（三选一）
  - 中间：运行进度日志（SSE 实时滚动）
  - 右侧：命中结果列表 + 可信度摘要
- 参数面板（每个技能展开后显示 V5 同款参数，有默认值，可输入）
- 命中列表（每只股票可展开证据）
- 失败摘要区（与命中列表同屏显示，默认折叠）

**不能做：**
- 不新建技能卡管理页面（工具箱是主页内嵌的折叠面板，不是独立页面）
- 不新建 explain 独立页面（折叠在命中列表里）
- 不做任意 DAG 编辑器

**验收：** 本章程第十三节的全部 15 条验收标准全部通过

**风险点：** 参数面板的 UI 设计是最容易滑向"越来越多字段 → 越来越复杂"的地方，必须严格控制第一阶段只暴露本章程第十一节列出的参数，不额外增加

---

#### 阶段 4（后续）：逐步扩张，按需解锁

以下内容在阶段 0-3 全部验收后，按需逐步添加，每次添加一项，每次添加必须有验收标准：

- 其余 21 张技能逐步接通真实数据
- 完整 12 种表达式操作符开放给高级用户（可选 UI 模式）
- 历史运行对比
- 综合评分体系
- 完整报告组格式
- 更多数据源（财务、事件等）

每一项扩张都必须先回答：**"这个能力能在主页上直接用吗，还是需要新建页面？"** 如果需要新建页面，必须有充分理由，且必须先确认主页的核心动线不因此受影响。

---

章程已完整输出，记忆已保存。

章程结构总览：
- **第一部分（第一至十三章）**：完整救场章程正文，覆盖定位、V6/V5处理方式、交付边界、技能管理、路径执行、数据拉取、实现原则、参数面板、报告输出、验收标准
- **第二部分：复用导向开发计划**，包含：
  - **V6资产继承表**（14项，每项写清直接引用/适配包装/只读参考/暂不碰）
  - **V5精华继承表**（14项，每项写清继承方式和禁止复制内容）
  - **对象接力开发表**（11步，从用户输入到报告输出的完整数据流，每步有输入/输出/复用/新写/验收）
  - **4阶段开发计划**（阶段0数据引擎→阶段1六个Producer→阶段2后端API→阶段3前端页面，每阶段有要做/不能做/验收/风险点）

章程的核心判断是：**先复用，后新增；先后端，后前端；先数据引擎，后执行引擎，后页面**。不允许从空白开始，每一步都明确哪里抄V5、哪里引V6、哪里需要新写。
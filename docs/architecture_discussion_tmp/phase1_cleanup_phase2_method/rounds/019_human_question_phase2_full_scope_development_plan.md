# Round 006 修正版: 第二阶段完整范围开发计划

本轮讨论输入文件：

```text
F:\v.6\v6-op\docs\architecture_discussion_tmp\phase1_cleanup_phase2_method\rounds\019_human_question_phase2_full_scope_development_plan.md
```

请 Claude 分析员把正式回复写入：

```text
F:\v.6\v6-op\docs\architecture_discussion_tmp\phase1_cleanup_phase2_method\rounds\020_claude_analyst_response_phase2_full_scope_development_plan.md
```

## 给 Claude 分析员

请忽略上一份 `017_human_question_phase2_two_pass_development_plan.md` 里关于“如果时间不够可以砍掉什么”的问法。这个问法已被人类否决。

本轮原则是：

```text
第二阶段目标只能给足，不能缩范围。
问题不在于 AI 模型能不能做多，而在于任务是否给清楚、拆解是否正确、验收是否明确。
```

请先读取：

```text
F:\v.6\v6-op\docs\architecture_discussion_tmp\phase1_cleanup_phase2_method\rounds\015_claude_analyst_response_phase2_scope_goals_alignment.md
F:\v.6\v6-op\docs\architecture_discussion_tmp\phase1_cleanup_phase2_method\rounds\016_codex_review_phase2_scope_goals.md
```

## 已确认的第二阶段 8 个大目标

请基于以下 8 个大目标制定完整开发计划，不要新增无关目标，也不要删减目标：

1. 参数-数据-缓存全链路一致化。
2. 全局解释层与分层归零诊断。
3. 来源层语义清晰化与诊断字段保留。
4. 问财授权验证与 Bridge 双角色定义。
5. 数据血缘层定义与取数透明化。
6. 主操盘台与管理中心分流。
7. 运行管理语义正式化。
8. 执行路径统一（图执行器）。

## 本轮要你输出什么

请输出一份可交给代码执行 Claude 的：

```text
第二阶段完整范围开发计划
```

要求：

- 优先判断能否一次完整开发完成。
- 如果你认为需要分多次，请说明分多次只是执行检查点，不是减少范围。
- 如果分两次或三次，每次也必须服务于完整第二阶段目标，不允许把大目标永久后置或删除。
- 不要输出四批、五批长期路线图。
- 不要说“砍掉什么”。
- 不要写代码，不要写施工命令。

## 请必须回答

### 1. 一次完整开发计划

请先给出“一次完整开发”的方案：

- 这一次开发覆盖哪些目标；
- 开发顺序是什么；
- 哪些模块先改、哪些模块后改；
- 前端、后端、报告、测试分别要做什么；
- 完成后用户能看到什么变化；
- 验收标准是什么。

### 2. 如果需要检查点，如何设置

如果你认为一次开发中需要设置检查点，请只按“检查点”表达，不按“删减范围”表达。

例如：

```text
检查点 1：结果可信与解释透明主链闭合。
检查点 2：管理分流、Bridge、数据血缘、运行管理闭合。
检查点 3：验收补漏与边界修正。
```

注意：检查点不是砍目标，不是缩范围。

### 3. 每个大目标的落地内容

对 8 个大目标逐项写：

- 需要改哪些主要模块；
- 需要新增哪些报告字段；
- 需要新增或调整哪些前端区域；
- 需要补哪些测试或验收；
- 与其他目标的依赖关系。

### 4. 完整验收清单

请给出完整验收清单，至少覆盖：

- 问财返回 0 / 45 / 接近上限时页面如何解释；
- 全A 5000 与问财 5000 如何区别显示；
- days 参数是否端到端生效；
- K线本地已有 / 新拉 / 失败 / 旧数据如何显示；
- 每个技能输入、命中、失败、筛空如何显示；
- 顺序 / 并行 / 混合路径如何解释；
- Data Provenance 如何进入报告；
- Bridge Capability 的两种模式如何定义；
- 主操盘台与管理中心如何分流；
- abort / 清缓存 / 历史报告 / 续跑的语义如何验收；
- 图执行器是否完成；如果暂不完成，必须给出明确原因和后续位置。

## 输出格式

请按以下结构输出：

1. 总判断：是否能一次完整开发完成。
2. 一次完整开发的总方案。
3. 如需检查点，检查点如何设置。
4. 8 个大目标逐项落地计划。
5. 涉及模块清单。
6. 前端变化清单。
7. 后端变化清单。
8. 报告/日志变化清单。
9. 测试与验收清单。
10. 最大风险与处理方式。

再次强调：

```text
不要砍目标。
不要缩范围。
不要输出长期路线图。
目标给足，计划写清楚。
```
